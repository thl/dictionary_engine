
<% edit_path = inline_edit_source_path(@source, :field => params[:field], :div_suffix => params[:div_suffix] ) 
   field = params[:field]
   div_suffix = params[:div_suffix]
%>	

<% if @source[field].blank?
  	t_content = 'Click to modify'
   else
	case params[:field] 
		when 'language_type_id' then 
			t_content = @source.language_type.title
		else
  			t_content = @source[field].s
	end	
   end %>
<%= link_to "#{t_content}".html_safe, edit_path, :class => 'definitions_show', :remote => true %>
