
<%# if !params[:update_div].blank? 
	div = params[:update_div] 
   end %>
<% edit_path = definition_edit_url(:id => @definition.id, :field => params[:field], :div_suffix => params[:div_suffix] ) 
   field = params[:field]
   div_suffix = params[:div_suffix]
%>	
<%# edit_path = definition_edit_url(:id => @definition.id, :update_div => div) %>
<% if @definition[field].blank?
  t_content = 'Click to modify'
else
	case params[:field] 
		when 'numerology'
			t_content = @definition[field]
		else
  			t_content = @definition[field].s  
	end
end %>
<%= link_to "#{t_content}".html_safe, edit_path, :class => 'definitions_show', :remote => true %>