
<% edit_path = inline_edit_oral_quotation_path(@oral_quotation, :field => params[:field], :div_suffix => params[:div_suffix] ) 
   field = params[:field]
   div_suffix = params[:div_suffix]
%>	

<% if @oral_quotation[field].blank?
  	t_content = 'Click to modify'
   else
	case params[:field] 
		when 'language_type_id' then 
			t_content = @oral_quotation.language_type.title
		else
  			t_content = @oral_quotation[field].s
	end	
   end %>
<%= link_to "#{t_content}".html_safe, edit_path, :class => 'definitions_show', :remote => true %>
